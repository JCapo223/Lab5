#include <iostream>
#include <vector>
#include <string> 
using namespace std;

int main()
{
    vector<char> intVector({ 'V','F','X','B','L','I','T','Z','J','R','P','H','D','K','N','O','W','S','G','U','Y','Q','M','A','C','E' });
    intVector[2];

    std::string(text);
    getline(cin, text);

    for (int I = 0; I < text.length(); I++) {
        text[I];

        if (text[I] >= 65 && text[I] <= 90)
            cout << intVector[text[I] - 65];
        else if(text[I] >= 97 && text[I] <= 122);
            cout << intVector[text[I] - 32];


    }

}